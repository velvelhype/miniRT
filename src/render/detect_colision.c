#include "miniRT.h"
#include "vector.h"

t_vecotr make_eyedir(t_vector eye_dir, t_vector)
{
	// t_vector screen_point;
	// screen_center = cam_pos + i * cam_vector;
	// screen_point = cam_ops + right_dir * x +  left_dir * y;
	// cam_dir = vec_sub(screen_point, cam_pos);
}

double	detect_colision(t_rt *rt_info, int x, int y)
{
	// printf("detect colision\n");
	// make eyedir;
	// detect_colision to all objs
	// 1:eye_dir and sphere
	// 2:with shadow
	// 3:with light
	// 4:with color
	// 2:eye_dir and multiple sphere
	// back forefront info
}